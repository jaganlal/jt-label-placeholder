'use strict';

/**
 * @ngdoc overview
 * @name jtLabelPlaceholderApp
 * @description
 * # jtLabelPlaceholderApp
 *
 * Main module of the application.
 */

 
angular.module('jtLabelPlaceholderApp', [
]);

/*
angular
  .module('jtLabelPlaceholderApp', [
    'ngAnimate',
    'ngCookies',
    'ngResource',
    'ngRoute',
    'ngSanitize',
    'ngTouch'
  ])
  .config(function ($routeProvider) {
    $routeProvider
      .when('/', {
        templateUrl: 'views/main.html',
        controller: 'MainCtrl'
      })
      .when('/about', {
        templateUrl: 'views/about.html',
        controller: 'AboutCtrl'
      })
      .otherwise({
        redirectTo: '/'
      });
  });
*/